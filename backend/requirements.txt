fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
pytest==8.3.3
# tools/migrate.py's driver, and app/db.py's connection pool (app/db.py is
# staged but not wired into any route yet - see backend/RECONCILIATION.md,
# 2026-08-17). Approved for the migration-runner mechanism and this staging
# only, not yet for actually cutting prediction_store.py over to Postgres.
psycopg[binary,pool]==3.2.3
